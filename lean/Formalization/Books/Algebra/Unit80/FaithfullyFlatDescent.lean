/-!
# Commutative Algebra, Chapter 80: Faithfully flat descent for projectivity of modules

The source section is an introduction to the subsequent descent argument.  It
contains no definition or theorem environment: its mathematical claims are
roadmap references to the later source sections named below.  Those later
interfaces are therefore not moved into this file.
-/

namespace Formalization.Books.Algebra.Unit80

/- The remaining assertions in the introduction are roadmap statements for
   later source sections rather than declarations at this source boundary:

   * a direct-sum/Kaplansky dévissage identifies a filtered module with the
     direct sum of its successive quotients;
   * projective modules decompose into countably generated projective modules;
   * countably generated projectivity is characterized by flatness and the
     later-defined Mittag--Leffler predicate;
   * that predicate descends along faithfully flat maps; and
   * the final proof filters an arbitrary module with projective base change by
     countably generated projective quotients and applies dévissage.

   These belong to the subsequent source sections and are intentionally not
   duplicated here.  The historical note about the error in the cited proof
   is mathematical context, not a separate formal assertion. -/

end Formalization.Books.Algebra.Unit80
