/-!
# Varieties, Chapter 2: Notation

The source section fixes the letter `k` as notation for the ground field used
throughout the chapter.  This is a standing naming convention, not a new
mathematical object or proposition.  Later declarations therefore use a
variable named `k` with Mathlib's existing `Field k` typeclass rather than
introducing a parallel ground-field definition.

The section contains no precise definition, assertion, displayed identity or
diagram, exact sequence, example, additional hypothesis, or mathematically
meaningful warning requiring a separate Lean declaration.
-/
